p Simple search component that fuzzy search given collection data such like json.

h2 default
.row
  .col-lg-4.col-md-6
    search(
      :collection='collection'
      :search-options='searchOptions'
      :result-limit='5'
      auto-width
      @item-clicked='eventTest'
      @submit='eventTest'
    )

h2 Example Search
.row
  .col-lg-4.col-md-6
    include ../partials/search-module

h2 Set Suggestions (Ajax Simulation)
.row
  .col-lg-4.col-md-6
    search(
      input-placeholder='input "tim"..'
      auto-width
      @input-change='onSearchInputChange'
    )
      li(
        slot='result-item'
        slot-scope='props'
        tabindex='0'
        @click='props.onClickItem(props.item)'
      )
        span(v-text='props.item.name')
