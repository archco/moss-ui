p Display inline or block fragment of computer code.

h2 Inline code
p
  | It is inline <code>code</code>.<br>
  | <code>HTML</code>, <code>CSS</code>, <code>JavaScript</code><br>
  | <a href="#"><code>code</code></a>  in link

h2 Code blocks
pre
  code
    |function getTarget() {
    |  const elm = document.querySelector('#target');
    |  return elm;
    |}

h2 Variables

p.pad
  <var>x</var> = <var>y</var> + 2 </p>

h2 User input

p.pad
  <kbd>ctrl + p</kbd> and <kbd>Enter</kbd>

h2 Sample output

p.pad
  <samp>This text is meant to be treated as sample output from a computer program.</samp>
